{% extends '::base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="span12 center">
            <div id="home-container">
                <h1 class="logo">Kateglo</h1>

                <form name="{{ data.form.name }}" class="form-search" action="{{ data.form.action }}"
                      method="{{ data.form.method }}">
                    <div class="input-append">
                        <input type="{{ data.form.input.search.type }}" name="{{ data.form.input.search.name }}"
                               class="input-xxlarge search-query"
                               placeholder="Masukkan kata, peribahasa atau singkatan...">
                        <button type="submit" class="btn">Telusuri</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
